name: publish

on: [push]

jobs:
  publish-fabric-server-image:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Login to GitHub Container Registry
      uses: docker/login-action@v1
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Build the Docker image
      run: |
        docker build . --tag ghcr.io/VirtualEriaLabs/fabric-server:latest
        docker run ghcr.io/VirtualEriaLabs/fabric-server:latest
        docker push ghcr.io/VirtualEriaLabs/fabric-server:latest
